# Link this app repo to its separate automation repo(s).
# Testward checks these for specs that break when this PR changes
# selectors / routes / endpoints they depend on.
automation_repos:
  - testward-qa/example-e2e
